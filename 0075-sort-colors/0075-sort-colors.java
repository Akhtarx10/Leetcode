class Solution {
    public void sortColors(int[] nums) {
        // int[] numsCopy = Arrays.copyOf(nums, nums.length);
        // Arrays.sort(numsCopy);
        // int[] ans = numsCopy;
        // return ans;
        Arrays.sort(nums);
    }
}